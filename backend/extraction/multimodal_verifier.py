from __future__ import annotations


def verify_photo_unavailable() -> dict:
    return {
        "photo_verification": None,
        "note": "Gemini multimodal Tier 2 demo not configured for this local run.",
    }

